## Getting Started

Welcome to my first java application. It's just a prompt calculator.

## Folder Structure

The workspace contains two folders by default, where:

- `src`: the folder where the app is.
- `lib`: the folder to maintain dependencies (but I don't use, this is for noobs).

Meanwhile, the compiled output files will be generated in the `bin` folder by default.
